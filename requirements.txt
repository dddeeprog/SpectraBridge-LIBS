# Python ≥ 3.9 建议
# 注意：torch 请按你的 CUDA/系统环境选择官方渠道安装，示例：
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision torchaudio
# 或使用 CPU 版：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# —— 核心 ——
torch>=2.2,<2.5
numpy>=1.24,<2.1
scipy>=1.10
scikit-learn>=1.3,<1.6
pyyaml>=6.0.1,<7.0

# —— 导出/部署 ——
onnx>=1.15,<1.17
onnxruntime>=1.17,<1.20
onnxsim>=0.4.36

# —— 训练与可视化 ——
tqdm>=4.66
matplotlib>=3.7
pandas>=2.0,<2.3
rich>=13.0
tensorboard>=2.13
typing-extensions>=4.8

# —— 可选（小工具）——
# einops 用于更易读的张量操作；若不需要可移除
einops>=0.6
